name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Dependencies
  - numpy
  - scipy
  - matplotlib

    # base python
  # - abc
  # - os
  # - argparse

    # Pip-only installs
  #- pip:
  #  - codecov
